# C1 方案设计 · 郭裕森

> 技术选型与模块分解。配套：`方案草案.md`（G1 送审）、`TRANSLATION_SPEC.md`（翻译规范）。

---

## 1. 总体架构

```
                      ┌──────────────────────────────────────────────┐
   一手来源            │  获取层（s01–s04）                            │
   ─────────           │  s01 官网     Next.js 客户端渲染 → JS 词法扫描 │
   themodernsoftware   │  s02 仓库     tar.gz + GitHub API 取 commit   │
   github.com/...      │  s03 阅读     6 级兜底链 + 正文产出量体检      │
   docs.google.com/... │  s04 讲义     Slides/Drive 无鉴权导出 → PDF   │
   cdn.openai.com/...  └───────────────────┬──────────────────────────┘
                                           ▼
                      ┌──────────────────────────────────────────────┐
                      │  语料层（s05 / s05b）                          │
                      │  HTML / PDF / Markdown → corpus/en/<slug>.md  │
                      │  统一形态：一个 H1 + 块序列（标题/段/列表/      │
                      │  代码/表格/引用）                              │
                      │  超长素材按块边界拆分 → 可稳定完成的翻译单元     │
                      └───────────────────┬──────────────────────────┘
                                          ▼
                      ┌──────────────────────────────────────────────┐
                      │  处理层（s06–s10b）                            │
                      │  s06 清点与覆盖率   s07 切段（稳定 ID）         │
                      │  s08 术语表         s09 翻译装配（可插拔）      │
                      │  s10 质检           s10b 术语回正（幂等）       │
                      └───────────────────┬──────────────────────────┘
                                          ▼
                      ┌──────────────────────────────────────────────┐
                      │  交付层（s11）                                 │
                      │  纯中文版 + 中英对照版 + 离线双语文档站         │
                      └──────────────────────────────────────────────┘
```

---

## 2. 模块分解与关键取舍

### 2.1 自研三件套（替换所有第三方依赖）

| 模块 | 行数 | 替换了 | 关键实现 |
|---|---:|---|---|
| `src/pdftext.py` | ~420 | PyMuPDF（AGPL） | 对象扫描 → FlateDecode → 内容流文本算子 → ToUnicode CMap → Form XObject 递归 → ObjStm 展开 |
| `src/htmlmd.py` | ~430 | trafilatura / readability | 轻量 DOM 树 → 样板剪枝 → 正文根打分 → 块切分 → 行内 Markdown |
| `src/jslit.py` | ~200 | 无头浏览器 | 手写词法扫描（识别字符串/模板串/注释/括号）→ 裸键名加引号 → JSON 解析，**不 eval** |

**为什么值得**：本机沙箱禁止 pip 写临时目录（路径含中文导致），第三方包一律装不上。
但这反而把项目推向一个更好的位置——**评审方 clone 下来零依赖即可运行**。

### 2.2 抓取兜底链（s03）

按**侵入性递增**排序，每一级独立缓存、独立记录：

| 级 | 策略 | 触发条件 |
|---:|---|---|
| 1 | `direct` | 默认，校验证书 |
| 2 | `insecure-tls` | 仅当报错含 SSL / CERTIFICATE |
| 3 | `alt-host` | 有官方替代端点（如 `arxiv.org` → `export.arxiv.org`） |
| 4 | `site-fallback` | `config/fallbacks.json` 里为该站点配置的一手替代来源 |
| 5 | `wayback` | 查 CDX，取最多 3 个快照，**挑正文最全的那个** |
| 6 | `reader-proxy` | 第三方文本代理（最后手段，会如实标注） |

**核心机制：正文产出量体检**。每一级取回后先跑一遍抽取，产出 < 1500 字符即判为"空壳"，
继续走下一级。**这是本管线与"只看状态码"的管线最本质的区别。**

### 2.3 五态分级（而不是 OK / FAIL）

| 状态 | 含义 | 后续动作 |
|---|---|---|
| `OK` | 正文正常取回 | 进入语料 |
| `SHELL` | 所有取法都只拿到很短的正文 | **仍进语料**，但在清点报告里如实标注 |
| `GONE` | 页面已不存在（原文删除/下线） | 记入已知缺口 |
| `FAILED` | 全部兜底路线失败 | 记入已知缺口，附完整报错链 |
| `SKIPPED` | 非文本素材（代码仓库/社交帖子等） | 登记不翻译 |

### 2.4 语料统一形态

所有素材（无论来自 HTML、PDF 还是 Markdown）统一成：

```
# 文档标题            ← 有且只有一个 H1

## 小节                ← 标题层级从 H2 开始

正文段落。

- 列表项
- 列表项

| 表头 | 表头 |
| --- | --- |

​```python
代码原样保留
​```

## Slide 12 / ## Page 7   ← PDF 分页锚点，原样保留
```

**为什么要统一**：只有形态一致，中英才能**逐块对齐**，质检才能做成机器可判定的。

### 2.5 切段与稳定 ID（s07）

每个最小翻译单元有稳定 ID：`<slug>#b0012`。这让：
- 翻译进度可按段统计（6,621 段待译）；
- 中英可逐段比对；
- 代码块被明确标记 `translate: false`，不进入翻译队列。

### 2.6 翻译的可插拔设计（s09）

```
plan      把语料切成分批计划（按优先级与字符预算打包）
status    报告完成度（按份 / 按段 / 按字符）
assemble  装配成品：纯中文版 + 中英对照版（含超长素材的部分合并）
api       可选后端：OpenAI 兼容接口（换源复用用，需要环境变量提供 Key）
```

**无论译文来自人、AI 逐段产出还是 API，只要落在 `corpus/zh/<slug>.md` 并通过结构校验，
下游一视同仁。** 这就是"换源复用"的技术保证。

### 2.7 质检（s10）与回正（s10b）

质检只回答**有确定答案**的问题：

| 类别 | 检查项 |
|---|---|
| 结构 | 块数 / 块类型 / 列表项数 / 表格行列数是否一一对应 |
| 代码 | 代码块是否逐字节未改 |
| 链接 | URL 集合是否完全一致 |
| 空译 | 空段落 / 照抄原文 / 明显过短 |
| 残留 | 中文段落里是否有成串未译英文（带"像不像散文"的判据） |
| 术语 | 定译命中数 vs 禁用写法命中数 → 一致率 |
| 排版 | 中英文之间是否漏空格（只认汉字，不含全角标点） |

回正是**数据驱动**的：`config/term_fix_rules.json`（12 条显式规则）
+ 从 `glossary.json` 的 `forbid` 自动派生（80 条），共 95 条。
三条硬约束：规则是数据不是代码、留痕永不覆盖、**幂等自检**。

---

## 3. 复现性设计

| 措施 | 作用 |
|---|---|
| 零第三方依赖 | 不需要 pip install，不需要虚拟环境 |
| 磁盘缓存 | 重跑不重复打服务器；也让离线复跑可行 |
| commit SHA 锚定素材 | 任何人任何时候都能取到**同一版本**的原文 |
| SHA-256 清单 | `sources/manifest.json` 记录每个素材的校验和 |
| 一键脚本 + 冒烟测试 | `scripts/reproduce.*` + `tests/test_smoke.py` |
| 幂等自检 | 术语回正跑第二遍必须零改动 |
| 运行账本 | `work/ledger.jsonl` 只追加，记录每步的时间/输入/输出/哈希 |

**明确做不到的**：译文本身不会在复现时"重新生成"——它是 LLM 产出并固化的产物。
复现的是**从原文到成品的全部自动化环节**：抽取、切分、校验、回正、建站、报告。
这一条我在 README 与 `拿来说明.md` 里都写明了，不含糊。

---

## 4. 风险与对策

| 风险 | 对策 |
|---|---|
| 站点结构变化导致解析失效 | 不硬编码压缩变量名，而是从 bundle 解析映射；失败时**报错退出而非静默取空** |
| 兜底链某一环空转 | 每环独立缓存 + 端到端复跑验证（这个坑真踩过） |
| 术语漂移 | 术语表冻结在前、回正在后；回正幂等自检 |
| 误伤正确译文 | 无法可靠判定的检查一律不做；规则误报立即撤 |
| 版权风险 | 分层授权；原始素材不入库；不引入任何 copyleft 依赖 |
| 环境限制（pip / 沙箱） | 零依赖自研，把限制变成设计约束 |
