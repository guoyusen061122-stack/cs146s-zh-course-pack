# C1 AI 开发日志 · 郭裕森

> 命名对照：挑战要求交付「AI 日志」。本文档即该交付物。
>
> **填写规则**：评分看的是"痕迹"——**你给了什么、AI 回了什么、你怎么判断的**。
> 本文档依据本会话的真实终端记录、真实报错原文与真实决策过程起草，**未美化、未删改失败**。
>
> **关于"我的判断"这部分（请先读这条）**：本文档的骨架（命令、报错原文、产出、发现与返工）
> 由 AI 依据真实终端记录整理；**文中所有标为「我的判断」的段落，都是我本人的判断**——
> 由我口述选择与理由，AI 只做了措辞整理，**没有添加我未表达过的观点**。
> 其中决策 4 那一处，我没有直接参与排查过程，写的是我事后对该结论的态度，
> 已在文中如实注明。
>
> 诚实交代一句：这轮工作里，管线设计、脚本实现、排错与质检主要由 AI 完成，
> 我承担的是**方向选择、取舍拍板与事后验证**。这份日志如实反映的就是这个分工。

---

## 总览

- **挑战**：C1 课程资料获取与翻译
- **开发区间**：2026-09-25（单日会话，多轮迭代）
- **主要工具**：DeepSeek Harness（deepseek-flash），工作目录 `cs146s-zh-course-pack`
- **目标课程**：Stanford CS146S《The Modern Software Developer》(Fall 2025 / Fall 2026)
- **产出**：89 项素材 / 77.6 万英文字符 / 110 份语料 / 6,000+ 翻译片段 /
  105 条术语表 / 13 步可复跑流水线 / 离线双语文档站

---

## 一、AI / 人 分工表

| 环节 | 谁做的 | 具体做了什么 |
|---|---|---|
| 确认目标课程与一手来源 | **人**（郭裕森）+ AI | 人确认"就是这门课"；AI 定位到官网与官方作业仓库 |
| 决定素材范围（是否扩到完整 49 项） | **人** | 看完标杆（93 分）的真实素材构成后，人选择"扩到完整官方素材集" |
| 决定翻译生产方式 | **人** | 人选择"可复现管线 + AI 产出译文"，而非调商业 MT API |
| 抓取管线设计与实现 | AI | s01–s04 全部脚本；多级兜底链；空壳体检 |
| PDF / HTML / JS 抽取器 | AI | 三个自研模块，替换掉 PyMuPDF(AGPL) / trafilatura / 无头浏览器 |
| 术语表 | AI 起草 | 105 条，含 80 条禁用写法；由人确认口径 |
| 译文产出 | AI（多路并行） | 39 个批次，每批自检到 0 问题 |
| 术语回正与质检 | AI | 规则表数据驱动 + 幂等自检 + 逐条留痕 |
| **AAR 的主观反思** | **人** | 见 `AAR.md`（七维复盘，本人填写） |
| **AI 日志中"我的判断"** | **人** | 见下文各决策节点的「我的判断」段落（共 9 处） |

---

## 二、关键决策节点

### 决策 1 · 2026-09-25 · 目标是哪门课？素材到底有多少？

**我的困境**：挑战只写"Stanford Vibe Coding 课程"，没有给链接。
搜索能搜到好几种说法（CS146S / CS194 / "斯坦福最火的 AI 编程课"），
而且课程资料散落在官网、Medium、各家技术博客、Google Slides、GitHub 里，**没有一份现成清单**。

**我让 AI 做的事**：先定位一手来源，再把"到底有多少素材"数清楚。

**AI 的回应（要点）**：

- 定位到官网 `themodernsoftware.dev`，标题即 **CS146S: The Modern Software Developer - Stanford University**；
- 作业仓库 `github.com/mihail911/modern-software-dev-assignments`，有 `fall2025` 与 `master` 两个分支
  （master 是当前学期，`pushed_at` = 2026-09-23，说明课程刚开学，只放出了 Week 1）；
- 更关键的发现：**官网是纯客户端渲染的 Next.js 应用**。抓下来的 HTML 里只有外壳，
  syllabus / FAQ / 评分标准全都不在里面，而是内联在 `/_next/static/chunks/*.js` 的压缩产物里。

**我的判断**：我一开始以为就是从课程官网把资料拿下来、用 AI 翻一遍。AI 摸完底才告诉我：
官网是动态页面，大纲和 FAQ 全在代码里；而且真正要处理的是 102 项、七种格式。
我接受“只用官网 + 官方仓库当一手来源”，是因为这两个地方的内容我能自己去核对，出了问题知道找谁；
用别人整理好的二手清单，出了问题没法追责，而且“怎么找一手资料”本来就是这道题要考的东西。

**结果**：从 bundle 里解出完整 syllabus —— Fall 2025 有 10 周 / 45 条阅读链接 / 14 份讲义链接，
Fall 2026 有 10 周（刚开学，多数周还是标题占位）。素材盘子这才第一次变得可数。

---

### 决策 2 · 2026-09-25 · 没有无头浏览器，客户端渲染的官网数据怎么取？

**我的困境**：syllabus 在压缩 JS 里。常规做法是起一个无头浏览器把页面渲染出来再抓 DOM，
但那要引入 200MB 级运行时，和"任何人 clone 下来就能跑"直接冲突。
更糟的是，直接对这团压缩代码做字符串替换极不可靠——变量名每次构建都会变。

**我让 AI 做的事**：在**不引入运行时依赖**的前提下把结构化数据取出来。

**AI 的回应**：写了一个**手写词法扫描器**（`src/jslit.py`）：自己识别字符串 / 模板串 / 注释 / 括号，
遇到 `{` 或 `,` 之后紧跟的裸标识符就给它加引号，再交给 JSON 解析。
**整个过程不 `eval`、不 `exec`**，压缩包里的任何代码都不会被执行。

更关键的一处设计：**它不从变量名取值，而是从 bundle 里解析出"学期 → 变量"的映射关系**再去取：

```
[映射] activeTerm === "fall2026" → weeks={'fall2026': 'eP', 'fall2025': 'eA'}
                                   grading={'fall2026': 'eT', 'fall2025': 'e_'}
                                   team={'fall2026': 'eC', 'fall2025': 'eS'}
```

**我的判断**：值。多写的这部分代码，换的是“以后不出声地取错数据”这个风险。
这门课官网的内容是动态生成的，里面的变量名每次改版都可能变；如果把名字写死，
改版之后脚本不会报错，只会安安静静取到错的东西——那比多花的时间代价大得多。

**结果**：站点重新构建导致变量名变化时，脚本仍能自证取对了学期，而不是静默取错。

---

### 决策 3 · 2026-09-25 · 装不了第三方包，16 份 PDF 的文本怎么办？

**我的困境**：讲义（13 份 Google Slides）和官方 PDF（Anthropic / OpenAI / arXiv）占了语料的一大块，
必须有 PDF 文本提取。但本机沙箱**禁止 pip 写临时目录**，任何第三方包都装不上：

```
ERROR: Could not install packages due to an OSError: [Errno 13] Permission denied:
'D:\DS Harness\DSH Desktop\工作区\cs146s-zh-course-pack\tmp\pip-unpack-i5uujvnv\pypdf-6.19.0-py3-none-any.whl.metadata'
```

我试过：重定向 `TEMP`/`TMP`/`TMPDIR` 到工作区、`--target` 装到工作区、`--no-cache-dir`，
全部失败。**根因是工作区路径含中文 `工作区`**，pip 用非 UTF-8 代码页处理会破坏路径。

**我让 AI 做的事**：评估"放弃 PDF vs 自己写提取器"，并给出理由。

**AI 的回应**：建议自己写，理由有两条，而且第二条是决定性的：

1. 覆盖度会直接塌掉（丢 16 份 PDF）；
2. **PyMuPDF 是 AGPL-3.0**，装进 MIT 仓库是明确的授权冲突 ——
   而"把 AGPL 作品装进 MIT 仓库"**正是本挑战三条红线之一**。

于是有了 `src/pdftext.py`：对象扫描 → FlateDecode（zlib 是标准库）→ 内容流文本算子 →
ToUnicode CMap 解码 → Form XObject 递归。

**我的判断**：值。为了省时间用一个协议冲突的工具，等于给仓库埋一颗雷，
以后谁想用这个仓库都得先处理这个麻烦。宁可自己多花时间写，换来的是许可干净；
而且还有一个额外好处：别人拿到这个仓库不用装任何东西就能跑。

**结果**：零第三方依赖 + License 干净。另见决策 4、5：自研过程本身又踩出两个真问题。

---

### 决策 4 · 2026-09-25 · 一份 23 页的 PDF 只抽出 27 个字符

**我的困境**：自研提取器在 Google Slides 导出的 PDF 上表现良好，但对 Anthropic 那份
23 页的官方 PDF，**只抽出 27 个字符**。而另一条路子（HTML 抓取）在同一篇文章上也几乎颗粒无收。

**我让 AI 做的事**：定位根因，不要猜。

**AI 的回应**：逐层排查后找到**两个独立的真原因**：

1. **这份 PDF 用单独的 `\r` 作换行符**，而 Python 的 `re.M` 只在 `\n` 之后认行首。
   我原先的对象扫描正则带了 `^` 锚定 → 4011 个对象只匹配到 1 个。
2. **页面字典被压缩进了对象流（ObjStm）**。更细的一层：对象流的
   **对象个数 N 与正文起始偏移 First 写在流的字典里**（`/N 23 /First 214`），
   不在解压后的数据开头 —— 我第一版把它们当成数据的头两个数字去解析，结果整份文档解出 0 页。

**我的判断**：印象最深的是：报错只告诉你结果（“只有 27 个字符”），真正的原因得一层层往下挖才找得到，
而且挖下去是两个**互不相干**的坑（换行符格式、以及数据被压缩进了另一种结构里）。
所以 AI 说“修好了”的时候，我不会只看它这句话，会看它到底验证了什么。

> *（如实注明：这一处的排查过程是 AI 做的，我没有直接参与，上面写的是我事后知道结论以后的态度。）*

**结果**：修复后同一份 PDF 抽出 **57,385 字符**。两处修复都写进了代码注释。

---

### 决策 5 · 2026-09-25 · "抓取成功"不等于"抓到了东西"

**我的困境**：跑完 46 条阅读链接的批量抓取，脚本报告 **38 条成功、0 条失败**。
但随手抽检几篇，发现有的页面 HTML 有 100KB+、状态码 200，正文却是空的
（内容全靠 JS 渲染，或者文章已经改成付费订阅只有预览）。
**只看状态码的管线，会把空壳当成成功收下。**

**我让 AI 做的事**：让"抓到了但没内容"这种情况必须自动触发兜底，而不是等到翻译阶段才发现。

**AI 的回应**：设计了**正文产出量体检**：每个 HTML 结果先跑一遍抽取，
产出不足 1500 字符就判定为"空壳"，继续走下一条兜底路线。配套的兜底链按侵入性递增：

```
direct → insecure-tls → alt-host → site-fallback(config) → wayback → reader-proxy
```

并且**每条路线用独立的缓存文件**（这条也很关键，见失败清单第 6 条）。

**我的判断**：对，宁可多一道检查，也不能让空壳混进成果里。不管的话，报告上会显示“38 条成功”，
但里面混着几条空的，等做到后面才发现没东西可翻——返工的成本比多一道检查高得多。

代价也有：有个 OWASP 的页面本身就很短（1367 个字符），也被判成了空壳。
我的处理是**保留内容、但在报告里标注出来**，而不是直接丢掉。这个代价我接受。

**结果**：这一改动直接救回 4 篇：

| 文章 | 原状态 | 修复后 |
|---|---|---|
| Good Context Good Code | 站点下线 | 经 Wayback 存档取回（15,382 字符） |
| Peeking Under the Hood of Claude Code | Medium 404 | 经 Wayback 存档取回（25,707 字符） |
| Prompt Engineering Guide | 客户端渲染空壳（144 字符） | 改用作者官方 GitHub Markdown（21,112 字符） |
| Claude Best Practices | 客户端渲染空壳 | 站点声明了 Markdown 原文，直接取干净全文（31,572 字符） |

同时新增了一个状态：`SHELL`（所有已知取法都只拿到很短的正文），
不再与 `OK` 混为一谈，并在清点报告里如实标注。

---

### 决策 6 · 2026-09-25 · 质检规则把 26 条正确译文判成了违规

**我的困境**：术语表把 `prompt` 定为「提示词」，同时把裸词「提示」列为禁用写法。
结果质检报告说某份讲义有 **14 处术语违规**。逐条看上下文，全是这些：

> 「零样本**提示**」「思维链**提示**」「K 样本**提示**」「自洽性**提示**」
> 「多轮**提示**」「角色**提示**」「**提示**：预览这个 markdown 文件的方法」

**26 条命中里，没有一条是错的。**

**我让 AI 做的事**：让它判断该"继续加豁免词"还是"撤掉这条规则"，并说明理由。

**AI 的回应**：建议**撤掉**。理由：

- 中文里 prompting 的正当译法本来就是「提示」，靠正则列举豁免词永远列不完；
- **一条误报会让二十条真报一起被忽略** —— 质检报告一旦有噪音，人就不看了，等于没做质检；
- 无法可靠判定的检查，**宁可不做**，改为用"术语缺失"这一提示性指标间接观察。

**我的判断**：有对有错。这条规则确实太严了——26 条报警里没有一条是错的，撤掉它我认为是对的：
一份有噪音的质检报告等于没做，人看两条假警报之后就不看了。

但“撤了可能导致其他影响”这个提醒我认：真正的名词误用确实可能漏掉。
所以撤规则的同时补了一个“术语缺失”的提示项——不判违规，但会列出来给人过目。

**结果**：同一轮里还发现另外两条同类误报并一并修正：

- `agent → 智能体` 禁用「代理」，但 **proxy 的正当译法就是「代理」**（反向代理）→ 改为带上下文的 `forbid_re` 正则；
- 中英文空格检查把**全角标点**也算成"中文"，导致「（fall2025 W3）」这种完全正确的写法被报错
  → 判据收紧为**只认汉字**。

---

### 决策 7 · 2026-09-25 · "兜底"配置了却完全没生效

**我的困境**：我给两个客户端渲染的站点配了专用替代来源（`config/fallbacks.json`），
可重跑之后那两篇**还是空壳**，而且报告显示走的是 `direct`。

**我让 AI 做的事**：查为什么配了兜底却没被使用。

**AI 的回应**：找到了一个非常隐蔽的实现缺陷——**所有兜底候选共用同一个缓存文件路径**。
第一次 `direct` 抓取已经把 HTML 写进了 `<slug>.html`；轮到 `site-fallback` 时，
`fetch()` 一看缓存文件存在，**直接把刚才那份 HTML 又读回来了**，
于是"兜底"实际上什么网络请求都没发。

修复：每条路线使用独立的缓存文件（`<slug>.site-fallback.md`、`<slug>.wayback.html` 等）。

**我的判断**：“配了”不等于“生效了”，这句话我这次记得很牢。兜底方案写得再全，
如果没跑一遍看结果，你根本不知道它到底有没有被用上。

这次的坑出在缓存：所有方案共用一个缓存文件，于是第二个方案直接读回了第一个的缓存，
**一个网络请求都没发出去**——而日志上看起来一切正常。

**结果**：修复后 `site-fallback` 与 `wayback` 两条路线才真正开始工作，救回上表那 4 篇。

---

### 决策 8 · 2026-09-25 · 术语回正差点把译文越改越烂

**我的困境**：译文由 39 个批次并行产出，同一概念出现两种写法（例如
`prompt engineering` 被译成「提示工程」也出现「提示词工程」）。于是做了术语回正步骤。
但我立刻想到一个更危险的问题：**回正规则自己会不会不幂等？**

**我让 AI 做的事**：让回正步骤自带幂等自检，并在机制上堵死不幂等的可能。

**AI 的回应**：自检确实抓到了问题 —— 规则「系统提示 → 系统提示词」在第一遍正确命中后，
**第二遍又会命中刚生成的「系统提示词」里面的「系统提示」**，产出「系统提示词词」。
同类还有「周作业 → 每周作业」。

修法不是在规则里打补丁，而是**机制**：把所有"正确写法"在替换前先送入保护区，
使它们不可能被二次命中。修复后自检输出：

```
[规则] 92 条（显式表 + glossary 派生），保护写法 50 个
[回正] 涉及 xx/xx 份，改动 N 处
[幂等] ✅ 通过（同一输入跑第二遍零改动）
```

**我的判断**：有价值。像“系统提示 → 系统提示词 → 系统提示词词”这种错，人眼看一百遍也看不出来，
因为改完的文件读起来是通顺的。只有让机器跑第二遍、发现“居然还能改”，它才会暴露出来。

**结果**：规则表最终 95 条（显式表 15 条 + 从术语表自动派生 80 条），
每次回正都把逐条 before/after 写进 `src/logs/term_fix_*.md`，**永不覆盖**。
（回正步骤后来还加了一道**防回归护栏**：只要 Markdown 块结构会变就拒绝写入 —— 见失败清单第 17 条。）

---

### 决策 9 · 2026-09-25 · 有一万两千字正文，从来没进过翻译队列

**我的困境**：项目已经跑到 102/109 份、质检只剩少数几个文件报错，看起来快收尾了。
但一个翻译批次回报了一个奇怪的观察：那份 Anthropic 的 Claude Code 文档，
**围栏代码块里装的其实是大段英文散文**（"块 #7 代码块被改动"这类告警）。

**我让 AI 做的事**：查清楚是译者把正文塞进了代码围栏，还是解析器认错了。

**AI 的回应**：是**解析器的错**，而且后果比表面严重得多。

那份文档用了**带属性的代码围栏**：

````
```txt title="claude (plan mode)" wrap theme={null}
````

而 `mdblocks.FENCE_RE` 原来的正则要求"语言标记后面直接是行尾"，**认不出这种围栏**，
于是把开围栏当成了闭围栏。连锁反应是：

1. 大段**正文散文被切成了"代码块"**（`__p1` 6 处、`__p2` 9 处，合计约 29,033 字符）；
2. 而在本流水线里，代码块被标记为 `translate: false`（"代码不该翻译"）；
3. 于是这 29,033 字符的正文**从未进入翻译队列**，也从未被任何人发现；
4. 更讽刺的是：译者按规则把"代码块"逐字节保留，**反而被判成"代码被改动"**。

**我让 AI 做的验证**：改动解析器会不会波及其他已对齐的译文？
AI 把新旧正则对全部 109 份语料跑了一遍并 diff 块序列，结论是**只有那 2 份受影响**
（`__p1` 47→89 块、`__p2` 35→99 块）。这个量化让我敢下决心改。

> 补充一个诚实记录：期间有一个批次向我报告"改这个正则会波及 27 份文件"，
> 我用实测推翻了这个数字（他们很可能用了更宽的正则）。**这说明子智能体的结论也必须复核，
> 不能因为它说得具体就当真。**

**我的判断**：第一反应是：这也太隐蔽了。这批内容不是“翻译得不好”，而是**根本没进翻译队列**——
因为一个规则认不出带属性的代码标记，把正文判成了代码，而代码按设计是不翻译的。
整个过程不报错，结构检查也全过。

所以我的结论是：自动化流程里最危险的错误不是“报错停住”，
而是这种“看起来一切正常、其实漏了一大块”。发现之后要做的是让 AI 继续改，
并且把这类检查补进流程，而不是改完就算。

**结果**：修正后那两份文件重译，`code_changed` 归零。

---

## 三、失败与返工清单（如实记录，共 18 条）

> 按时间顺序。每条都写清楚"症状 → 根因 → 怎么发现 → 怎么修"。**没有删除任何一条。**

| # | 症状 | 根因 | 怎么发现的 | 怎么修的 |
|---|---|---|---|---|
| 1 | `pip install` 一律失败 | 工作区路径含中文，pip 用非 UTF-8 代码页处理会破坏路径；沙箱另禁写临时目录 | 打印完整报错，看到路径被破坏成 `������` | 放弃第三方依赖，改为零依赖自研 |
| 2 | 阅读抓取只得到 1 个文件 | 我用 `Select-Object -First 2` 截断输出，**PowerShell 提前终止管道把 Python 进程杀了** | 复查目录发现 raw 里只有 1 个文件 | 改用 `Select-Object -Last` / 完整输出后再过滤 |
| 3 | 4 个视频被合并成 2 条 | **我自己引入的回归**：`urlparse` 已剥掉 `?`，我却用 `[?&]v=` 去匹配，匹配不到就把 query 清空，不同视频塌缩成同一个 key | 发现 `video=4` 变成 `video=2` | 正则改为 `(?:^\|[?&])v=` |
| 4 | 23 页 PDF 只抽出 27 字符（第 1 因） | 该 PDF 用单独 `\r` 换行，`re.M` 的 `^` 不认，4011 个对象只匹配到 1 个 | 逐层排查：先数 `/Type /Page` 出现次数 | 去掉 `^` 锚定 |
| 5 | 23 页 PDF 只抽出 27 字符（第 2 因） | 页面字典在对象流（ObjStm）里；而 `/N` `/First` 在**流的字典**里，不在解压后的数据开头 | 打印解压后的头部字节，发现开头直接是成对数字 | 从字典读 N/First，再从数据前 `First` 字节解析成对数字 |
| 6 | 配了兜底来源却完全不生效 | 所有候选**共用同一个缓存文件**，第二次尝试直接读回第一次的缓存 | 报告显示空壳却走了 `direct` | 每条路线独立缓存文件 |
| 7 | Medium 的 Wayback 存档抽出的是乱码 | Wayback 的 `id_` 快照会原样返回当年服务器发出的 **gzip 字节**，未解压 | 报错 `UnicodeDecodeError: 0x8b in position 1` | 统一在读取入口做透明 gunzip（含缓存读取路径） |
| 8 | GitHub Blog 整页 19,700 字符，剪完只剩 86 | 我的样板过滤器按 class 特征命中了一个"把正文也包进来"的外层容器，**连正文一起删了** | 抽检时发现该篇只有 42 字符 | 加安全阀：命中样板特征但含 1200+ 字符正文的节点不剪 |
| 9 | 讲义文件名变成 `...teamshtml.html` | slug 生成时点号被替换、扩展名重复保留 | 浏览素材目录时发现 | 统一 `url_slug()`，先去扩展名再规范点号 |
| 10 | `glossary.json` 瞬时 JSON 解析失败 | **多路并行翻译期间我还在改术语表**，有批次正好读到写了一半的文件 | 子智能体上报 `Expecting ',' delimiter: line 12 column 5` | 术语表定稿后冻结；把这个并发风险写进 AAR |
| 11 | 术语回正把链接改坏 | 保护 URL 时把整个 `](url)` 都塞进占位符，还原时多套了一层括号 | 幂等自检报"第二遍仍产生改动" | 只保护 URL 本身，不连定界符一起塞 |
| 12 | 全局质检报告被覆盖成 0 问题 | 翻译批次按规范自查时跑 `s10_qa.py --slug X`，脚本却把结果写进全局报告 | 发现 `_qa.json` 里 issues 变成 0 | 单文件模式改写到 `work/qa_<slug>.json`，不碰全局报告 |
| 13 | **约 2.9 万字正文从未进入翻译队列** | 带属性的代码围栏（```` ```txt title="…" theme={null} ````）让解析器把**开围栏当成了闭围栏**，正文被切成"代码块"，而代码块按设计不翻译 | 翻译批次回报"代码块被改动"的异常告警 | 修正 `FENCE_RE` 让信息串可带属性；实测确认只影响 2 份文件后重译 |
| 14 | 一次"优化"差点让语料不可复现 | 我想把样板剪枝的安全阀从"只看文字量"改成"文字量 + 链接密度"，但这等于**动了语料生成器** —— 语料已冻结、译文已按它逐块对齐，重跑就会产出不同语料 | 冒烟测试报"样板剪枝安全阀失效"，顺着查才发现自己引入了逻辑反向错误 | **回滚代码**，只在注释里写明"此函数不可事后调整"；新增 `tests/test_reproducible.py` 把这条约束变成可执行的检查 |
| 15 | 质检规则反过来改变了译文形态 | 旧的"连续 ≥8 英文单词即视为未译"判据把**合法英文引用**判成漏译，于是有译者为了把指标打到 0，**压缩/改写了原文引用、调整了 markdown 星号分布** | 逐条复核各批次的交付报告 | 从质检器一侧修掉假阳性（新增 `kept_english` 指标 + 参考文献/引文豁免），并通知所有在跑的批次**把原文改回原样** |
| 16 | **四个交付文档被一条批量替换命令改花** | 我用 PowerShell 单行命令批量改文档时，**PowerShell 把"单元素数组"展平成字符串**，`$pair[0]` 取到的成了**第一个字符** —— "整串替换"退化成"单字替换"：`C1`→`C2`、`CS146S`→`CS246S`、`README`→`REIDME`、`失败`→`败败` | 替换后立即复查，发现数字和文字都不对 | 四个文件（方案草案 / AAR / 群消息留痕 / 文件对照表）**全部重写**；教训：批量改动必须用能保证原子性的方式，且**改完立刻验证** |
| 17 | **9 份译文被"术语回正"改坏**（274 个质量问题） | 我在回正步骤里多写了一句 `re.sub(r" {2,}", " ", body)`（折叠连续空格）。而 unit42 系列的正文是「PDF 抽取后压平的表格」，**靠成百上千个填充空格维持几何** —— 折叠空格直接摧毁了它的块结构与链接。**在靠空格排版的内容里，空白就是数据。** | `--apply` 之后立刻重跑质检，问题数从 5 暴涨到 274 | ① 删掉那句折叠空格的代码；② 给回正加**防回归护栏**：只要 Markdown 块结构发生变化就**拒绝写入**；③ 派修复批次重做那 9 份 |
| 18 | **一份讲义被静默覆盖，从未进入翻译队列** | 同一节课的 Google Slides 讲义与 Drive 上的「设计文档模板」由 `slide_slug()` 生成了**完全相同的文件名**，后写的覆盖了先写的。索引里两条都在，磁盘上只有一个文件 —— 而且索引里的两个条目各自带着**不同且都已过时的字符数**，从数字上完全看不出异常 | 收尾核对"语料索引 110 条 / 磁盘 109 个文件"的差额时发现重复 slug | ① 在 s05 加**全局 slug 唯一性兜底**（撞名即追加内容特征后缀，并打日志，不静默处理）；② 把被覆盖的讲义恢复为独立文件并补译；③ 设计文档的译文归位到新 slug |

---

## 四、AI 在哪最帮我、在哪误导我

### 最帮我的三处

1. **不厌其烦地"打印事实"而不是"猜"**。第 4、5、7、8 条失败，全都是靠"把中间数据打出来看"
   而不是靠推理定下来的。AI 在这件事上没有偷懒。
2. **把一次性排查沉淀成机制**。空壳体检、幂等自检、独立缓存、留痕日志——
   都不是"修好这一次"，而是"以后不会再犯"。
3. **主动说出能力边界**。视频字幕抓不到时，它没有假装成功，而是把
   YouTube 要求 pot 令牌、四种 InnerTube 客户端全被拒、沙箱禁 pip、yt-dlp 独立程序无法解包
   这四条证据一并列出。

### 最误导我的五处（后两处是我自己造成的破坏）

1. **第一版样板过滤器过度自信**。它按 class 特征"聪明地"剪掉了样板，
   结果把 GitHub Blog 的正文一起剪了（19,700 → 86）。教训：**过滤器的误删比漏删危险得多**，
   因为它不报错。
2. **第一版质检规则过严且不自知**。26 条"术语违规"全是误报，
   如果我没有逐条看上下文，就会拿着一个假数字去改本来正确的译文。
3. **兜底链的设计漏洞**。写了六级兜底、配了站点专用来源，
   但因为共用缓存，"兜底"整整一轮都是空转——**看起来做了很多，实际一点没生效**。
4. **"顺手清理空白"是危险的**。我在术语回正里加了一句折叠连续空格的代码，
   本意是"让排版干净点"，结果把 9 份靠空格定宽的译文改坏（问题数从 5 暴涨到 274）。
   根因是把"多余的空白"当成了噪声 —— 而在靠空格排版的内容里，**空白就是数据**。
   现在回正步骤加了**防回归护栏**：只要 Markdown 块结构会变，就拒绝写入。
5. **工具本身会咬人**。我用一条 PowerShell 命令批量改文档，因为 PowerShell 把
   "单元素数组"展平成字符串，`$pair[0]` 取到的是第一个字符，
   于是"整串替换"变成了"单字替换"，把 `C1` 改成 `C2`、`README` 改成 `REIDME`，
   四个文档被改花、只能全部重写。

**这五处的共同点：都不报错，或者报错的地方离真正的原因很远。**
所以配套的机制不是"更仔细地 review"，而是：
**留痕 + 幂等自检 + 防回归护栏 + 端到端复跑 + 改完立刻验证。**

---

## 五、可复核的原始记录

| 证据 | 位置 |
|---|---|
| 每一步的运行账本（时间、输入、输出、哈希） | `work/ledger.jsonl`（只追加，永不覆盖） |
| 素材来源与 SHA-256 清单 | `sources/manifest.json` |
| 抓取策略与失败原因逐条 | `sources/readings/index.json`、`sources/docs/index.json` |
| 术语回正逐条明细 | `src/logs/term_fix_*.md` ｜ 摘要 `reports/_term_fix_log.md` |
| 质检逐条问题 | `reports/_qa_scan.md` |
| 清点与覆盖率 | `reports/_inventory.md` |
| 翻译批次自查明细 | `work/qa_<slug>.json` |
