# C1 拿来说明 · 郭裕森

> 命名对照：挑战要求交付「拿来说明」。本文档即该交付物。
>
> 它同时承担三件事：**① 复用声明与 License 合规；② 换源复用指南（怎么把这套管线用到别的课）；
> ③ 使用与交接说明书（别人拿到这个包怎么读、怎么跑、怎么改）。**

---

## 1. 复用声明（我拿了什么，逐项标 License）

### 1.1 实际复用的清单

| # | 来源 | 取用了什么 | License / 授权状态 | 我做了什么改动 |
|---|---|---|---|---|
| 1 | **Stanford CS146S 官网** `themodernsoftware.dev` | 总览、大纲、FAQ、评分构成（两个学期） | 版权归课程组，公开可访问，学习用途 | 无改动，仅结构化提取 |
| 2 | **官方作业仓库** `mihail911/modern-software-dev-assignments` | Fall 2025 与 master 分支的文档与代码 | 版权归 Mihail Eric / Stanford，公开仓库 | 无改动，按 commit SHA 锚定 |
| 3 | **课程指定的阅读文章**（32 篇第三方文章） | 正文（用于翻译） | 各归原作者，公开可访问 | 无改动；译文见 `LICENSE-CONTENT.md` |
| 4 | **官方与嘉宾讲义**（16 份） | Google Slides / Drive 导出的 PDF 文本层 | 版权归 Stanford 与 Cognition / Anthropic / Graphite / Vercel / Resolve AI / Warp | 无改动，仅提取文本层 |
| 5 | **Wayback Machine**（archive.org） | 2 篇**原站已下线**文章的存档快照 | 存档服务条款（Internet Archive） | 标明取自存档，未伪装成原站内容 |
| 6 | **`dair-ai/Prompt-Engineering-Guide`** | 1 篇客户端渲染页面的 **Markdown 原文** | **MIT** | 直接取用，未改动 |
| 7 | `r.jina.ai` 文本代理 | 兜底链最后一级（**本次未实际用到**） | 第三方服务 | 仅保留配置与说明 |

### 1.2 我**没有**拿来的（同样重要）

| 没拿 | 原因 |
|---|---|
| 任何第三方翻译引擎（Argos Translate 等） | 本机装不上（见 §1.4）；且改用 LLM 直译质量更高 |
| 任何无头浏览器（Playwright / Selenium） | 与"clone 下来零依赖可跑"的目标冲突 |
| **PyMuPDF** | **AGPL-3.0，装进 MIT 仓库是授权冲突**（挑战红线之一） |
| `trafilatura` / `readability` | 同上，改为自研 `htmlmd.py` |
| 别人的译文 / 别人整理好的素材清单 | 一手来源必须自己定位，见 `AI日志.md` 决策 1 |
| 商业 MT API 的产出 | 项目主题是"可复现"，商业 API 无法保证他人复现同一批译文 |

### 1.3 分层授权（本仓库的 License 结构）

```
LICENSE              → 流水线代码（src/ scripts/ tests/）：MIT
LICENSE-CONTENT.md   → 译文与原文素材（corpus/ translated/ glossary/ sources/）：版权归原作者
```

**为什么分层**：代码是我的，译文不是（原作者的）。把两者混在一个 License 下，
要么让代码无法被自由复用，要么让原作者的版权被稀释。分层是唯一诚实的做法。

### 1.4 一处主动披露的风险（不藏）

本项目的**第一大设计约束不是技术选择，而是环境**：本机沙箱禁止 pip 写临时目录，
且工作区路径含中文 `工作区`，pip 会用非 UTF-8 代码页破坏路径。

```
ERROR: Could not install packages due to an OSError: [Errno 13] Permission denied:
'...\工作区\cs146s-zh-course-pack\tmp\pip-unpack-i5uujvnv\pypdf-6.19.0-py3-none-any.whl.metadata'
```

我试过重定向 TEMP/TMP/TMPDIR、`--target` 装到工作区、`--no-cache-dir`，全部失败。
**这件事的后果是被如实写进了设计**：整条流水线零第三方依赖。
它的**好处**是评审方不需要装任何东西就能复现；**代价**是我得自己写 PDF / HTML / JS 三个抽取器
（合计约 1,400 行），并因此踩出对象流、CR 换行等一批真坑（这些都写进了 AI 日志）。

---

## 2. 怎么读这个包（5 分钟上手）

### 2.1 只想读中文

1. 双击 `site/index.html` —— 离线双语文档站，左侧目录选题，右上角可切换「仅中文」；
2. 或者直接看 `translated/<slug>.zh.md`（纯中文）。

### 2.2 想核对译得准不准

看 `translated/<slug>.bilingual.md`（逐块中英对照）。
每段中文紧跟对应英文，段落一一对应，不需要来回切文件。

### 2.3 想看流程是否可信

| 想看什么 | 打开 |
|---|---|
| 有多少素材、缺了哪些、为什么 | `reports/_inventory.md` |
| 译得怎么样（结构/代码/链接/术语） | `reports/_qa_report.md` ｜ 逐条问题 `reports/_qa_scan.md` |
| 术语改过什么、为什么改 | `reports/_term_fix_log.md` ｜ 逐条明细 `src/logs/term_fix_*.md` |
| 每一步跑了什么、输入输出哈希 | `work/ledger.jsonl` |
| 素材来源与校验和 | `sources/manifest.json` |

---

## 3. 换源复用指南（把这套管线用到另一门课）

这是本包**最想让下一位同学用上的部分**。整条管线的设计前提就是"翻译是可插拔的一步"。

### 3.1 只有三种情况需要改代码

| 情况 | 要改哪里 | 例子 |
|---|---|---|
| 新站点也是客户端渲染、数据在 JS 里 | `src/s01_fetch_site.py` 的映射解析（或直接提供结构化 JSON） | 本项目对 Next.js 的处理 |
| 新站点的反爬/下线方式没被覆盖 | `config/fallbacks.json` 加一条（**只改配置**） | promptingguide.ai 改用官方 GitHub 原文 |
| 版式与新的 Markdown 方言冲突 | `src/mdblocks.py` 的块解析 | 中文全角 `）` 作为有序列表标记 |

**其余全部不用改**。

### 3.2 五步换源

```powershell
# 1) 把新课程的素材抓下来（照抄 s01–s04 的结构，或直接用现成文件）
# 2) 语料抽取 → corpus/en/<slug>.md（统一成「一个 H1 + 若干块」的形态）
python src/s05_extract_corpus.py
python src/s05b_split.py          # 超长素材按块边界自动拆分

# 3) 重做术语表：先跑扫描拿到高频候选词
python src/s08_glossary.py        # 会输出 reports/_term_candidates.md
#    然后编辑 glossary/glossary.json —— 这是唯一需要人认真做的部分

# 4) 翻译：分批计划 → 逐批产出 corpus/zh/<slug>.md → 装配
python src/s09_translate.py plan
python src/s09_translate.py status
python src/s09_translate.py assemble

# 5) 质检 → 回正 → 建站
python src/s10_qa.py
python src/s10b_fix.py --apply
python src/s11_build_site.py
```

### 3.3 如果你有 LLM API Key（全自动路径）

`src/s09_translate.py api` 提供了 OpenAI 兼容后端。配置三个环境变量即可全自动跑完翻译：

```
CS146S_LLM_BASE=https://your-endpoint/v1
CS146S_LLM_KEY=sk-...
CS146S_LLM_MODEL=gpt-4o-mini
```

**注意**：本仓库交付的译文**不是**用这条路生成的（本环境没有可用 Key）。
这条路径是为了"换源复用"保留的——下一门课可以直接全自动跑完。
无论走哪条路，只要译文落在 `corpus/zh/<slug>.md` 并通过 `s10_qa.py` 的结构校验，
下游装配与建站完全一视同仁。

### 3.4 术语表的维护方式（这是最值得抄的部分）

`glossary/glossary.json` 每条术语有四个关键字段：

| 字段 | 含义 | 作用 |
|---|---|---|
| `zh` | 中文定译 | 唯一答案 |
| `forbid` | 明令禁止的写法 | **质检脚本咬得动**——只写"应该译成什么"是查不出问题的 |
| `forbid_re` | 带上下文的正则级禁用 | 处理"同一个词在不同词性下译法不同"（如 `prompt` 名词 vs prompting） |
| `keep` | 保留英文不译 | 产品名、缩写、文件名 |

**关键经验**：`forbid` 列表必须同时冻结"不许译成什么"。
但**不要为了拦截而写会误伤的规则**——本项目撤掉过一条把 26 条正确译文判成违规的规则，
理由写在 `reports/_qa_report.md`。**一条误报会让二十条真报一起被忽略。**

---

## 4. 待办与已知不足（交接给下一位）

| # | 待办 | 说明 |
|---|---|---|
| 1 | ~~`AI日志.md` 与 `AAR.md` 的本人判断部分~~ | ✅ **已完成**：15 处（AI 日志 9 + AAR 6）由本人口述、AI 仅整理措辞，未添加本人未表达过的观点。其中 2 处（AI日志决策 4、AAR 维度三）属推导而非直接口述，已在文中如实注明 |
| 2 | **G1 背书需要在群里公开发起** | 见 `G1背书.md`。《总则》要求背书必须公开发起，私聊/邮件/线下不被接受 |
| 3 | **官方 AAR 模板如可得，请按模板重排** | 本次七维框架是自拟的，素材无需改动 |
| 4 | 视频字幕（4 个） | 装好 yt-dlp 后按同流程接入，管线无需改动 |
| 5 | 401 讲义 1 份 + Drive 文件 3 份 | 需课程方开放权限 |
| 6 | `Specs Are the New Source Code` 只有付费墙预览 | 原文已转付费订阅 |
| 7 | GitHub 发布 | 见 §5，本机未安装 git，推送需你在本地完成 |

---

## 5. GitHub 发布（本机未装 git，步骤如下）

```powershell
# 1) 安装 git（本机当前没有 git，这是发布的前置条件）
winget install --id Git.Git -e

# 2) 在仓库目录初始化并提交
cd cs146s-zh-course-pack
git init -b main
git add .
git commit -m "C1: CS146S 中文课程资料包（89 项素材 / 77.6 万字符 / 13 步可复现流水线）"

# 3) 建远端并推送（先到 GitHub 网页建一个空仓库，不要勾选 README）
git remote add origin https://github.com/<你的用户名>/cs146s-zh-course-pack.git
git push -u origin main
```

**发布前请确认**：

- `.gitignore` 已排除 `sources/`（原始素材 40MB+，且版权不属于你，不宜再分发）。
  别人 clone 后按 §3.2 的 s01–s04 重新抓取即可，抓取脚本已在仓库里。
- 仓库根目录放了 `LICENSE`（MIT，只覆盖代码）与 `LICENSE-CONTENT.md`（译文与素材版权）。
- 若你的 GitHub 账号是免费版，仓库建议设为 **Public** 以便评审访问；
  译文属学习用途的非官方译本，已在 `LICENSE-CONTENT.md` 中声明。

---

## 6. 关键决策的"原文 / 指令 / 产出 / 对比"

挑三个最能说明问题的决策，把原始输入与产出摆出来。

### 6.1 从压缩 JS 里取 syllabus（不 eval、不依赖变量名）

**输入**：`https://themodernsoftware.dev/` 的 HTML（25,866 字符）与 9 个 JS chunk。

**问题**：syllabus 不在 HTML 里，而在 `0vbqnwtk.bs09.js` 中，形如：

```js
eA=[{title:"Week 1: Introduction to Coding LLMs and AI Development",
     topics:["Course logistics","What is an LLM actually","How to prompt effectively"],
     reading:[{label:"Deep Dive into LLMs",href:"https://www.youtube.com/watch?v=..."}],
     sessions:[{date:"Mon 9/22:",lead:"Introduction and how an LLM is made",
                links:[{label:"Slides",href:"https://docs.google.com/presentation/d/..."}]}]},...]
```

**产出**：

```json
{
  "term_mapping": {
    "active_literal": "fall2026",
    "weeks":   {"fall2026": "eP", "fall2025": "eA"},
    "grading": {"fall2026": "eT", "fall2025": "e_"},
    "team":    {"fall2026": "eC", "fall2025": "eS"}
  },
  "terms": { "fall2025": {"weeks": [ ...10 周... ], "faq": [...7 条...] } }
}
```

**对比**：起无头浏览器要多 200MB 依赖且不可离线；硬编码变量名会在站点重建后静默取错学期
（取错也不报错——正是本项目最警惕的失败形态）。当前方案两者都避开了。

### 6.2 抓取兜底链：配了却不生效

**输入**：`config/fallbacks.json` 里为 `www.promptingguide.ai/techniques` 配了官方 Markdown 替代来源。

**第一版产出**：重跑后该篇**仍是空壳**，报告显示走了 `direct`。

**根因**：所有兜底候选共用同一个缓存文件路径，第二次尝试直接命中第一次写下的缓存，
**网络请求根本没发出去**。

**修复后**：

```
OK   html  site-fallback  21126B  Prompt Engineering Guide  https://www.promptingguide.ai/techniques
OK   html  wayback       214220B  Specs Are the New Source  https://blog.ravi-mehta.com/p/...
OK   html  wayback       184403B  Peeking Under the Hood...  https://medium.com/@outsightai/...
```

**对比**：修复前 3 篇是空壳/乱码，修复后合计取回 42 万字节正文。这是"看起来做了很多、
实际一点没生效"的典型——只有端到端复跑才能揭穿。

### 6.3 术语回正：从"系统提示词词"说起

**输入**：规则「系统提示 → 系统提示词」，以及一份含「系统提示」的译文。

**第一版产出（不幂等）**：

| 遍次 | 文本 |
|---|---|
| 原文 | …配置**系统提示**… |
| 第 1 遍 | …配置**系统提示词**… ✅ 正确 |
| 第 2 遍 | …配置**系统提示词词**… ❌ 越改越烂 |

**修复（机制而非补丁）**：把所有"正确写法"在替换前先送入保护区，使它们不可能被二次命中。

**修复后自检输出**：

```
[规则] 92 条（显式表 + glossary 派生），保护写法 50 个
[幂等] ✅ 通过（同一输入跑第二遍零改动）
```

**对比**：靠人眼抽检几乎不可能发现"提示词词"这种错——它不报错、不影响结构、
只是悄悄变差。**幂等自检是这类问题的唯一可靠防线。**

---

*本文件即「拿来说明」交付物。文档站、术语表、质检报告等其余产出见 `README.md` 的演示入口表。*
