# 变更记录

按「每次有意义的提交一条」记录。本包在单日会话内完成，故按**阶段性里程碑**记录。

---

## 2026-09-25 · 获取层（s01–s04）

- 新增 `src/jslit.py`：手写 JS 词法扫描器，从 Next.js 压缩产物中安全取出结构化数据（不 eval）
- 新增 `src/s01_fetch_site.py`：抓官网两个学期，解析出「学期 → 压缩变量」映射后取
  syllabus / FAQ / 评分构成 / 团队；产出 `sources/site/course.json`
- 新增 `src/s02_fetch_repo.py`：抓作业仓库两个分支的 tar.gz，并用 GitHub API 记录
  **commit SHA**（可复现锚点）
- 新增 `src/s03_fetch_readings.py`：批量抓阅读素材，**6 级兜底链** +
  正文产出量体检 + 五态分级（OK / SHELL / GONE / FAILED / SKIPPED）
- 新增 `src/s04_fetch_media.py`：Google Slides / Drive 无鉴权导出 → PDF；视频字幕尝试并记录失败证据
- 新增 `config/fallbacks.json`：站点专用一手替代来源（逐条写理由）
- **修复**：YouTube 归一化正则误清空 `v=` 参数，导致 4 个视频塌缩成 2 个（自己引入的回归）
- **修复**：兜底候选共用缓存文件 → 兜底空转（配了站点来源却完全没生效）
- **修复**：Wayback 快照是 gzip 字节未解压 → 抽出 3.5 万"字符"全乱码
- **修复**：样板过滤器误删正文（GitHub Blog 整页 19,700 字符剪完只剩 86）

## 2026-09-25 · 语料层（s05 / s05b）

- 新增 `src/pdftext.py`：**自研零依赖 PDF 文本提取器**，支持 FlateDecode、ToUnicode CMap、
  Form XObject 递归、**对象流（ObjStm）展开**
- 新增 `src/htmlmd.py`：**自研 HTML → Markdown**，含样板剪枝、正文根打分、div 版式兜底
- 新增 `src/s05_extract_corpus.py`：全部素材统一成 `corpus/en/<slug>.md`
- 新增 `src/s05b_split.py`：超长素材按块边界拆分（6 份 → 27 个翻译单元）
- **修复**：PDF 用 CR-only 换行导致对象扫描只匹配到 1 个对象（4011 → 1）
- **修复**：ObjStm 的 `/N` `/First` 在流的字典里而非数据开头 → 整份 PDF 解出 0 页；
  修复后同一份 PDF 从 **27 字符 → 57,385 字符**
- **修复**：HTML 里的孤立代理字符（`&#xD83D;`）导致写盘崩溃 → 出口统一净化
- **修复**：拆分算法只在 `## ` 切分，遇到"某标题下 5 万字"仍产出 5.6 万字块 → 改按最小可切单元累积

## 2026-09-25 · 处理层（s06–s10b）

- 新增 `src/mdblocks.py`：Markdown → 结构化块，中英对齐与质检的共同基础
- 新增 `src/s06_inventory.py`：清点与四种覆盖率口径
- 新增 `src/s07_segment.py`：切段，稳定 ID `<slug>#b0012`
- 新增 `glossary/glossary.json`：**105 条术语表**，含 `zh` / `forbid` / `forbid_re` / `keep`
- 新增 `src/s08_glossary.py`：术语表自检（重复、定译冲突、禁用写法与定译打架）+ 生成人读版
  + 从语料扫高频词反查漏收术语（930 个候选）
- 新增 `src/s09_translate.py`：`plan / status / assemble / api` 四个子命令；产出
  **纯中文版 + 中英对照版**（审阅意见明确要求补纯中文版）
- 新增 `src/s10_qa.py`：七类机器质检；**单文件模式不污染全局报告**
- 新增 `src/s10b_fix.py` + `config/term_fix_rules.json`：**数据驱动的术语回正**，
  95 条规则（15 条显式 + 80 条派生），**幂等自检**，逐条留痕永不覆盖
- 新增 `TRANSLATION_SPEC.md`：翻译硬性规范 R1–R10（所有译者必读）
- **修复**：术语规则过严，把 26 条正确译文判成违规 → **撤掉规则**并写明理由
- **修复**：`agent` 的禁用词「代理」误伤 proxy 的正当译法 → 改为带上下文的 `forbid_re`
- **修复**：中英文空格检查把全角标点算成"中文" → 判据收紧为只认汉字
- **修复**：链接保护逻辑把 `](url)` 整体塞进占位符 → 还原时多套一层括号
- **修复**：回正规则不幂等（产出「系统提示词词」）→ 机制上保护"正确写法"

## 2026-09-25 · 交付层（s11 + 文档）

- 新增 `src/s11_build_site.py`：**离线双语文档站**（逐块对照、一键切换、全文检索、
  纯 `file://` 可用、不引用任何 CDN）
- 交付文档：`README.md`、`AI日志.md`、`AAR.md`、`拿来说明.md`
- 过程与合规：`方案草案.md`、`方案设计.md`、`G1背书.md`、`Portfolio条目.md`、
  `LICENSE`（MIT，仅代码）、`LICENSE-CONTENT.md`（译文与素材版权）
- 质量与自检：`docs/红线自测.md`、`docs/文件对照表.md`、`docs/群消息留痕.md`
- 可复现：`scripts/reproduce.ps1`、`scripts/reproduce.sh`、`tests/test_smoke.py`、`.gitignore`

## 2026-09-25 · 返工与加固（含 3 次自己造成的破坏）

> 这一节记录的**全部是我自己的失误**，未做任何美化。教训比成绩更值得留档。

### 解析器缺陷：2.9 万字正文从未进入翻译队列

- **发现**：翻译批次回报"代码块被改动"的反常告警
- **根因**：`mdblocks.FENCE_RE` 认不出带属性的代码围栏（```` ```txt title="…" theme={null} ````），
  把开围栏当成闭围栏，导致大段**正文散文被判成"代码块"**；而代码块按设计不翻译
- **影响**：2 份文件、约 29,033 字符正文从未被翻译
- **修复**：放宽 FENCE_RE 的信息串匹配；并把闭围栏标记改为**完整围栏串**
- **验证**：先用新旧正则对全部 109 份语料 diff 块序列，实测**只有 2 份受影响**，才动手改

### 质检器假阳性 → 反过来改变了译文形态

- **现象**：旧判据"连续 ≥8 英文单词即视为未译"把**合法英文引用**判成漏译
  （论文页眉、OCR 残留原文、要求模型复述的原始提示词、参考文献条目）
- **后果**：有译者为了把指标打到 0，**压缩/改写了原文引用**
- **修复**：新增 `kept_english` 提示性指标 + 参考文献/引文豁免；
  并通知所有在跑的批次把原文改回原样
- **原则**：**一条误报会让二十条真报一起被忽略**，质检宁可少报也不能错报

### 回正步骤误伤 9 份译文（最严重）

- **原因**：我在 `s10b_fix.py` 里多写了一句 `re.sub(r" {2,}", " ", body)`（折叠连续空格）
- **后果**：unit42 系列的正文是「PDF 抽取后压平的表格」，**靠成百上千个填充空格维持几何**，
  折叠空格直接摧毁了块结构与链接 —— 问题数从 5 暴涨到 274
- **修复**：① 删掉折叠空格的代码；② 给回正加**防回归护栏**（块结构会变就拒绝写入）；
  ③ 派修复批次重做那 9 份
- **教训**：回正步骤只该做**定向替换**；在靠空格排版的内容里，**空白就是数据**

### 一份讲义被静默覆盖（收尾核对时才发现）

- **现象**：语料索引有 110 条，磁盘上只有 109 个文件，且索引里出现**重复 slug**
- **根因**：同一节课的 Google Slides 讲义与 Drive 上的「设计文档模板」被 `slide_slug()` 生成了
  **完全相同的文件名**，后写的覆盖了先写的 —— **一份讲义就这么静默消失，从未进入翻译队列**
- **为什么难发现**：索引里两条都在，各自带着不同且都已过时的字符数，从数字上看不出任何异常
- **修复**：① s05 加**全局 slug 唯一性兜底**（撞名即追加内容特征后缀并打日志，不静默处理）；
  ② 恢复被覆盖的讲义为独立文件并补译；③ 设计文档的译文归位到新 slug
- **教训**：**"索引条数"和"磁盘文件数"必须对得上**。这个差额检查是发现静默丢失的最后一道防线

### 四个文档被批量替换命令改花

- **原因**：PowerShell 把"单元素数组"展平成字符串，`$pair[0]` 取到的是**第一个字符**，
  "整串替换"退化成"单字替换"：`C1`→`C2`、`CS146S`→`CS246S`、`README`→`REIDME`、`失败`→`败败`
- **修复**：四个文件（方案草案 / AAR / 群消息留痕 / 文件对照表）**全部重写**
- **教训**：破坏性改动之后必须**立刻验证产出**

### 新增的可复现性验证

- `tests/test_reproducible.py`：重跑 s05 + s05b，语料必须**逐字节相同** —— 当前实测 **109/109 相同**
- 这条测试的意义：抽取器一旦产出语料、语料一旦与译文对齐，就不能再随意改。
  改一行抽取逻辑 = 换一份语料 = 全部译文错位

## 待办

- [x] `AI日志.md` / `AAR.md` 的本人判断部分（15 处：AI 日志 9 + AAR 6）—— **已由本人填写完毕**
- [ ] G1 背书公开发起 + 截图留痕
- [ ] 拿到官方 AAR 模板后按模板重排（内容不变）
- [ ] 装好 yt-dlp 后补齐 4 个视频字幕
- [ ] GitHub 推送（本机未装 git）
