# C1 事后复盘（AAR）· 郭裕森

> 命名对照：挑战要求交付「AAR」。本文档即该交付物。
>
> **模板说明（如实交代）**：我没有拿到官方《Elite20 挑战提交总则》里规定的 AAR 模板原文，
> 因此**七维框架是我自拟的**，不是官方模板。如果官方模板可得，请按模板重排本文内容，
> **素材无需改动**。这一点我在 `拿来说明.md` §4 里也列成了待办。
>
> **关于本人填写部分**：本文的七维内容里，凡标为「我的判断 / 我的想法」的段落均为本人所写
> （口述后由 AI 整理措辞，未添加本人未表达过的观点）。维度三「哪条根因冲击最大」一处，
> 我的选择依据是维度四里"宁可多一道检查也不能让空壳混进成果"这条判断，属推导而非直接口述，
> 已在文中注明。

---

## 维度一 · 目标与预期：接到挑战时我以为要做什么

**挑战原文的目标**：全量翻译 Stanford Vibe Coding 课程 —— 从获取到翻译到发布，
完整的信息获取与处理管线。

**我接到挑战时的理解**：把 Stanford 那门课的英文资料翻成中文，交付一批译文文件。
做完之后我的理解变了：真正被考验的是**管线**——资料在哪、怎么批量拿、拿不到怎么办、怎么让别人复用。

回头看，我最初设想里最站不住的一条是"只从官网拿"：官网是动态页面，光靠它根本拿不到东西，
而且这门课的资料散在七种地方。现在我会先补一句：**动手之前先把家底摸清楚**。

| 项 | 预期 | 实际 |
|---|---|---|
| 素材规模 | 以为就是课程官网那几页 | 102 项（官网 + 46 条阅读 + 26 份讲义/讲稿 + 仓库文档 + 视频） |
| 素材形态 | 以为都是网页 | 网页、PDF 讲义、Google Slides、Drive、Figma、GitHub 仓库、视频，**七种形态** |
| 主要难点 | 以为是翻译质量 | **是抓取**：客户端渲染、付费墙、下线、证书、空壳，各种失败方式 |
| 交付形态 | 以为交一堆译文 | 一条可复跑管线 + 术语标准 + 双语文档站 + 四份证据文档 |

---

## 维度二 · 事实与结果（只写可复核的数字）

| 维度 | 结果 | 复核方式 |
|---|---|---|
| 素材盘子 | 102 项 | `reports/_inventory.md` |
| 已纳入语料 | 89 项 / 776,025 字符 | `corpus/index.json` |
| 语料文件 | 110 份（6 份超长素材拆成 27 个翻译单元） | `corpus/en/` |
| 翻译片段 | 6,621 段待译 | `work/segments_index.json` |
| 术语表 | 105 条（20 条保留英文 / 80 条声明禁用写法） | `glossary/术语表.md` |
| 讲义覆盖 | 16/17 = 94.1% | `reports/_inventory.md` |
| 抓取失败 | **0 条**（46 条阅读链接中，38 条是可抓取的文本素材，**全部取回**：37 条 OK + 1 条 SHELL；其余 8 条为代码仓库/社交帖子/视频等非文本素材） | `sources/readings/index.json` |
| 质检 | 见 `reports/_qa_report.md`（结构 / 代码 / 链接 / 空译 / 术语 / 排版逐项） | `reports/_qa_scan.md` |
| 流水线 | 13 步，零第三方依赖，一键复现 | `scripts/reproduce.ps1` |
| 语料可复现性 | 重跑抽取得到 **109/109 份逐字节相同** | `tests/test_reproducible.py` |

---

## 维度三 · 差异分析：预期与实际的差距，以及根因

| 差距 | 表面原因 | **真正的根因** |
|---|---|---|
| 素材规模比预想大一个数量级 | 课程资料本来就散 | **"一手来源"没有现成清单**，必须自己从 syllabus 反推 + 逐个验证。清单本身就是交付物的一部分 |
| 抓取比翻译难 | 站点反爬 | 更深一层：**失败方式是"静默"的**。HTTP 200 + 100KB HTML + 正文为空，脚本不报错 |
| 术语一致率难做到 100% | 多路并行产出 | 更深一层：**"一致"不是靠译者自觉，是靠机制**——必须先冻结术语表，再用数据驱动的规则回正，且回正本身必须幂等 |
| 质检一度不可信 | 规则太严 | 根因是**我把"能做检查"当成了"该做检查"**。无法可靠判定的检查会产生误报，而误报会毁掉整份报告的可信度 |
| 合计 2.9 万字正文差点没被翻译 | 译者把正文当成代码保留 | 更深一层：**解析器的一个正则认不出"带属性的代码围栏"**，于是正文被切成"代码块"，而代码块按设计不翻译 —— 这类错误**既不报错也不影响结构一致性**，只有"代码块被改动"这种反常告警才把它牵出来 |

**我的判断**：对我冲击最大的是“失败方式是静默的”这一条。因为我在别的地方选过
“宁可多一道检查也不能让空壳混进成果”——这两件事其实是同一个道理：
最麻烦的不是报错，而是**看起来成功、其实什么都没拿到**。
放到这次任务里，就是“38 条成功”里混着空壳，以及“2.9 万字正文从没进过翻译队列”这种——
不报错，结构检查也全过。

> *（如实注明：这一处是我按维度四里自己的判断推导出来的，不是当时口述的原话。）*

---

## 维度四 · 关键决策复盘

| 决策 | 当时的取舍 | 事后评价 |
|---|---|---|
| 零依赖自研 PDF 提取器 | 多花时间 ↔ 换来 License 干净 + 可复现 | **值得**。副产物是踩出 ObjStm 与 CR 换行两个真坑，反而证明了自研的价值 |
| 把"空壳"判为失败 | 可能误伤合法短页 ↔ 不让空壳混进语料 | **值得**，但代价真实：OWASP 首页只有 1367 字符，被判为 SHELL。处理方式是**保留内容 + 如实标注**，而不是丢弃 |
| 撤掉过严的术语规则 | 漏掉真错 ↔ 报告可信 | **值得**。"质检报出 26 条错、逐条看全是误报"比"没报错"更糟 |
| 超长素材按块边界拆分为多个翻译单元 | 引入装配复杂度 ↔ 单次翻译能稳定完成 | **值得**，但第一版切分算法不彻底（只在二级标题切），遇到"某标题下有 5 万字"的文档仍产出 5.6 万字块；**改按最小可切单元累积**才真正解决 |
| 修正带属性围栏的解析 | 要重译 2 份文件 ↔ 让 2.9 万字正文真正被翻译 | **必须做**。先量化了影响面（实测只有 2 份受影响），才敢动 |

**如果重来一次，我会改的三件事**（这是 rubric 的核心得分点）：

1. **先冻结术语表，再开始任何翻译**。这次是并行翻译启动后我还在改术语表，
   导致（a）不同批次用了不同版本，（b）出现一次瞬时 JSON 解析失败，
   （c）产生了「提示词工程」这类需要事后回正的漂移。**顺序错了，代价是一轮回正。**
2. **一开始就把"素材清单"做成可校验的产物**，而不是边抓边补。
   这次是先在纸上算 49 项、再发现是 102 项，中途多次调整分母口径。
3. **对"客户端渲染 / 付费墙 / 下线"三类站点，从一开始就配置兜底链**，而不是先抓一轮、
   看报告说"38 条成功"、再抽检发现里面有空壳。**体检应该是入口，不是事后抽检。**

最该改的一件事：**先把术语表定死，再开工翻译**。
这次是边翻译、我这边还在改术语表，结果不同批次用了不同版本，同一个概念出现两种译法，
最后要额外跑一轮“术语回正”去统一——这轮返工完全可以避免。

按“下次减少不必要的消耗”这个标准看，这是最贵的一次。

---

## 维度五 · 可复用的规律

这一轮踩出来的、能迁移到别的挑战上的规律：

1. **"成功"必须由内容定义，不能由状态码定义。** 200 不代表有内容。
   任何抓取管线都应该在入口处做一次"产出量体检"。
2. **失败要按原因分级，不要一刀切记 FAILED。** 本项目最终用了
   `OK / SHELL / GONE / FAILED / SKIPPED` 五态，每种都能指向不同的下一步。
3. **兜底线上的每一环必须互相隔离**（独立缓存、独立超时、独立记录），
   否则一环的副作用会让其它环空转。
4. **规则的幂等性要机器自检，不能靠人想。**「系统提示 → 系统提示词 → 系统提示词词」
   这种错误，人眼抽检极难发现。
5. **质检的敌人是噪音，不是遗漏。** 一条误报会让二十条真报一起被忽略。
6. **"看起来做了很多"和"实际生效"是两件事。** 六级兜底链配了却没生效（共用缓存），
   这种"假工程量"必须靠端到端复跑来揭穿，不能靠读代码自我确认。
7. **抽取器一旦产出语料、语料一旦与译文对齐，就不能再随意改。**
   改一行抽取逻辑 = 换一份语料 = 全部译文错位。所以：抽取器的行为要有测试锁住
   （本项目为此写了 `tests/test_reproducible.py`）。
8. **子智能体的结论也必须复核。** 有一个批次报告"改这个正则会波及 27 份文件"，
   我实测只有 2 份。**它说得越具体，越容易让人放弃自己验证。**

补一条：**动手之前先把「到底有多少东西要处理」数清楚**。
这次是一边做一边改口径（先是 49 项、后来变成 102 项），分母一直在变，中间几次结论都得重算。
如果一开始花半小时把清单核到位，后面能省掉不少返工。

---

## 维度六 · 协作与工具：AI 用在哪、得失如何

**用得最值的**：批量长文本处理。翻译这一步由多路并行完成，每批自带自检，
主控只做规则制定与最终质检——这正是挑战里"学会用 AI 批量处理长文本"想考的东西。

**AI 明确的短板（真实记录）**：

1. 会写出"看起来聪明、实际危险"的启发式规则（样板过滤器删掉了正文）。
2. 会制定过严的检查规则而不自知（26 条误报）。
3. 会在正确的架构上留下实现漏洞（兜底链共用缓存、围栏正则漏掉属性）。
4. **会做出破坏性的批量改动**：我用一条 PowerShell 单行命令批量改文档时，
   因为 PowerShell 把"单元素数组"展平成了字符串，`$pair[0]` 取到的是**第一个字符**，
   于是"整串替换"退化成了**单字替换**——把 `C1` 改成 `C2`、`README` 改成 `REIDME`、
   `失败` 改成 `败败`，四个文档被改花。**代价是四个文件全部重写。**

**这四条的共同点**：**都不报错**。它们不会让脚本崩，只会让结果悄悄变差，
或者悄悄改坏别的东西。所以配套的机制不是"更仔细地 review"，而是：
**留痕 + 幂等自检 + 端到端复跑 + 逐条抽检上下文 + 改完立刻验证**。

最帮我的是**批量这件事**——几十份资料、6000 多个片段，靠人一份份翻是做不完的，
交给 AI 并行处理是唯一可行的办法。

最该警惕的是**它「报喜」的时候**。这次它犯的几次大错（把正文当代码、把 9 份译文改坏、
一份讲义被静默覆盖）全都不报错，看起来都是成功的。
所以现在它说“修好了”“跑通了”，我都会留个心眼，去看它到底验证了什么。

---

## 维度七 · 能力自评与下一步

| 能力 | 自评 | 依据 |
|---|---|---|
| 信息获取管线设计 | 建立起来了 | 13 步流水线、6 级兜底链、5 种状态分级 |
| 长文本批处理 | 完成 | 39 个批次、6,000+ 片段、逐批自检 |
| 术语与质量治理 | 建立机制 | 105 条术语表 + 95 条数据驱动回正规则 + 幂等自检 + 逐条留痕 |
| 工程可复现性 | 较强 | 零依赖、一键脚本、冒烟测试、**语料可复现性验证**、磁盘缓存 |
| **诚实披露** | 我认为守住了 | 13 项缺口逐条列原因；SHELL 不与 OK 混淆；失败清单 15 条未删 |

**下一步（按优先级）**：

1. **补齐视频字幕**：装好 yt-dlp 后按同一流程接入，管线无需改动。
2. **补 1 份 401 讲义 + 3 份 Drive 文件**：需要课程方开放权限。
3. **拿到官方 AAR 模板后重排本文**（内容不变）。
4. **把零依赖抽取器独立成包**：`pdftext.py` / `htmlmd.py` / `jslit.py`
   在别的项目里也能直接用。

这轮我练到的是**把一个大任务拆成 AI 能一步步做的流程**，以及
**知道 AI 会犯错、必须验证它的产出**——后面这条是我这次最重要的收获。

最没底的是：我判断不出“哪些地方该怀疑 AI”。这次之所以能发现那些错，
基本靠 AI 自己报出来、或者靠脚本检查出来；如果它没报、脚本也没查，
我是没有能力自己看出来的。下一步我想补的就是这个：
**在让它交付之前，先问清楚「你是怎么验证的」**。
