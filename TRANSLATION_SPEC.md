# CS146S 中文翻译规范（TRANSLATION_SPEC）

> 本文件是**所有译者（人或 AI）必须遵守的硬性约定**。
> 目的很单纯：让 89 份、6,000 多个片段由多路并行翻译产出后，读起来像**同一个人翻的**。
>
> 配套文件：术语表 `glossary/glossary.json`（译名唯一事实来源）、
> 机器校验 `src/s10_qa.py`（本规范的每一条都尽量做成了可自动检查的规则）。

---

## 0. 一句话任务

把 `corpus/en/<slug>.md` 译成中文，写到 `corpus/zh/<slug>.md`，
**结构逐块对齐、代码一字不动、术语严格按表**。

---

## 1. 硬性规则（违反即判不合格，机器可查）

### R1 结构一一对应
输出必须与原文**块对块对齐**：同样的标题层级、同样数量的段落/列表项/表格行、同样的顺序。
不许合并段落、不许拆分段落、不许新增小标题、不许省略任何一段。
（`s10_qa.py` 会逐块比对，数量或类型不一致直接报错。）

### R2 代码一字不动
所有 ``` 围栏代码块、行内 `` `代码` ``、命令、文件路径、变量名、API 名**原样保留**，
一个字符都不改，包括注释也可以保留英文。
（机器校验：译文里的代码块与原文 **逐字节相同**。）

### R3 链接地址不动
`[文字](URL)` 里的 **URL 一律不变**，只译「文字」部分。
URL 集合必须与原文完全一致（机器校验）。

### R4 术语严格按表
`glossary/glossary.json` 里的词必须使用其 `zh` 字段的定译；
`forbid` 列出的写法**绝对不许出现**；`keep: true` 的词保留英文不译。
首次出现时可写成「中文（English）」，之后只用中文定译。

### R5 中英文之间加空格
「使用 Claude Code 完成」✅ ｜「使用Claude Code完成」❌
数字与中文之间同理：「3 个智能体」✅。

### R6 标点用中文全角
句末用「。」「？」「！」，句内用「，」「；」「：」；
但**代码、URL、英文本身内部**的标点保持原样。
引号统一用「」，书名/产品名不加书名号。

### R7 不许留未译的整句英文
除 `keep: true` 的专有名词外，正文里不应出现成串的英文句子。
（机器校验：连续 ≥8 个英文单词的中文段落会被标记。）

### R8 不加译注、不删内容
不要写「译者注」「注：」这类插入语（必要的澄清写进交付文档，不写进译文）。
不要因为「这句不重要」就略过——原文有的，译文必须有。

### R9 表格逐格翻译，行列数不变
表头与每个单元格都要译，行数、列数与原文一致。

### R10 只改文字，不改编排
不加粗、不加删除线、不调整标题级别、不把列表改成段落。

---

## 2. 风格要求（没有硬性对错，但决定读感）

| 项 | 要求 |
|---|---|
| 口吻 | 技术文档口吻，克制、直接。不客套、不煽情、不用感叹号。 |
| 句长 | 英文长句拆成 2–3 个中文短句。**中文一句话超过 60 字就该拆**。 |
| 直译 / 意译 | **直译优先**。技术文本里「忠实」比「优雅」重要，宁可句子平实也不要走样。 |
| 被动语态 | 英文被动改中文主动或无主句。「The file is written by the agent」→「由智能体写入该文件」或「该文件由智能体写入」。 |
| 代词 | 少用「它」「他们」，必要时重复名词，避免指代不明。 |
| 英文原词 | 首次出现的术语可在中文后括注英文，如「上下文工程（context engineering）」，之后不再重复。 |
| 标题 | 标题译成短语，不加句号。疑问句标题保留问号。 |
| 列表项 | 与原文保持同一语法层级；原文是名词短语就别译成完整句子。 |
| 数字与单位 | 数字不变；`10-12 hours` →「10–12 小时」。 |

---

## 3. 禁止的机器腔（必须改掉）

| ❌ 机翻腔 | ✅ 应写成 |
|---|---|
| 它是重要的是 | 重要的是 |
| 这允许你去…… | 这使你可以……／这样就能…… |
| 在这种情况下 | 此时／这种情况下 |
| 一个用户的一个需求 | 用户的一个需求 |
| 通过使用……来…… | 用……即可…… |
| 这是被设计来…… | 该设计用于…… |
| 让我们来看看 | 来看 |
| 我们需要注意的是 | 需要注意 |

---

## 4. 特殊情况怎么处理

- **口语化的演讲/访谈稿**（嘉宾讲义）：可适度口语化，允许保留讲者的语气，但不要加「哈哈」「呃」这类填充词。
- **PPT 讲义**：`## Slide N` 这类分页标记**原样保留**（它是定位锚点，不是正文）。每页里的要点译成简短词组，不要扩写成句子。
- **PDF 论文**：`## Page N` 标记原样保留。摘要、图表标题、参考文献条目都要译（参考文献的作者名与标题保留英文）。
- **产品名 / 人名 / 公司名**：保留英文（Mihail Eric、Anthropic、Vercel）。
- **无法翻译的双关或缩写**：保留英文并在中文后加简短说明，例如「YOLO（一次做完不回头）」。
- **原文本身有错别字或语法错误**：照原意译通顺即可，不要标注。

---

## 5. 交付与自检

产出文件：`corpus/zh/<slug>.md`，UTF-8，LF 换行。

译完请自查：

1. 第一行是不是 `# 中文标题`，且与原文 H1 对应？
2. 段落数、列表项数、表格行列数是否与原文一致？
3. 代码块是否**一个字符都没动**？
4. 所有 URL 是否都还在、没被改动？
5. 有没有出现 `glossary.json` 里 `forbid` 的写法？
6. 有没有成串没译的英文句子？
7. 中英文之间有没有漏空格？

第 2–7 条可以用一条命令机器复核：

```bash
python src/s10_qa.py --slug <slug>
```
